# STALE — Do not use

This is a stale library inventory. The authoritative DOX for vendored libraries is at `libraries/AGENTS.md` in the active repo root.

If this backup folder still exists, its contents may differ from what's on disk. Do not rely on it for library inventory or build instructions.
